program: benchmark_kron.py
method: grid
project: kronecker-benchmarks
parameters:
  MATRIX_SIZE:
    distribution: int_uniform
    min: 1
    max: 300
  implementation:
    values: ["TorchKroneckerLayer", "torch.kron"]
